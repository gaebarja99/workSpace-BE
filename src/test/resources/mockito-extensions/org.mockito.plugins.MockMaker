# FR-003 테스트: Mockito 기본(inline) mock-maker는 JDK 21+에서 java agent self-attach가 필요한데,
# 이 환경의 Gradle 캐시 경로에 비ASCII 문자가 포함돼 agent 로딩이 실패한다(MockitoInitializationException).
# 우리 테스트는 final 클래스/정적 메서드를 목킹하지 않으므로(인터페이스/일반 클래스만), agent가 필요 없는
# subclass mock-maker로 전환한다.
mock-maker-subclass
